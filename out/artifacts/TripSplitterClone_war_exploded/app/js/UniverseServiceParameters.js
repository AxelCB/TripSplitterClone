//the only thing this does is define the URL in a global variable
var universeServiceUrl = 'http://localhost:8080/universe-core';
var supported = ['es', 'pt'];
var defaultLang = 'es';
var canSubscribe = false;