//the only thing this does is define the URL in a global variable
var tripSplitterCloneServiceUrl = 'http://localhost:8080/core';
var supported = ['es', 'pt'];
var defaultLang = 'es';
var canSubscribe = false;